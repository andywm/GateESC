#pragma once
/*
class Adafruit_SSD1306;

class DialerProgram
{
  int Address[9] = {0};

  void AddSymbol(int Symbol);
  void BeginDial();
  void Process();
};

struct DialerUI
{
	enum class AnimPhase {SymbSmall, SymbBig, SymFull, Pane, Reset};

	void Iniitalise(Adafruit_SSD1306* InDisplay);
	void DrawStaticElements();
	void SetChevron(int Chevron, bool bOn);
	void SetSymbolPane(int Chevron, int Symbol);
	void SetBigSymbol(int Phase, int Symbol);
	void SetBigButton();
	void TickAnimation();

	unsigned long PrevTime = 0;
	int CurrentSymbol = 0;
	int MaxSymbol = 0;
	bool bBigButton = false;

	AnimPhase Phase = AnimPhase::SymbSmall;
	Adafruit_SSD1306* Display = nullptr;
};

*/